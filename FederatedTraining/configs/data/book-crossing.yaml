# @package _global_

# to execute this experiment run:
# python train.py experiment=example

DATA:
  root: ../dataset
  name: 'book-crossing'

FED:
  num_clients: 228
  agg_epochs: 400
  local_epochs: 2