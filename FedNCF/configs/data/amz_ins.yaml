# @package _global_

# to execute this experiment run:
# python train.py experiment=example

DATA:
  root: ../dataset
  name: 'amazon-industry-and-science'

FED:
  num_clients: 240
  agg_epochs: 400
  local_epochs: 2